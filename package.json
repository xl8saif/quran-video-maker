{
  "name": "quran-video-maker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "python3 scripts/prepare-recitation-timings.py && node scripts/prepare-mushaf.mjs && node scripts/prepare-local-fonts.mjs && vite",
    "test": "vitest run tests",
    "build": "python3 scripts/prepare-recitation-timings.py && node scripts/prepare-mushaf.mjs && node scripts/prepare-local-fonts.mjs && vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "lucide-react": "latest",
    "react": "latest",
    "react-dom": "latest",
    "typescript": "latest",
    "vite": "latest"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "vitest": "latest"
  }
}
