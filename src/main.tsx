import React from 'react'
import { createRoot } from 'react-dom/client'
import { Play, Pause, Upload, Settings2, Music2, Hand, Languages, RotateCcw, BookOpen } from 'lucide-react'
import './styles.css'
import { mushafStyles, defaultMushafStyle, type MushafStyleId } from './mushafStyles'
import { seedQuran } from './quranData'

const surahs=['Al-Fatiha','Al-Baqarah','Yasin','Al-Mulk','Al-Ikhlas','Al-Falaq','An-Nas']
const highlightColors={gold:'#d9bd63',green:'#9bcf8a',yellow:'#f0dc55',amber:'#e5a84b'}
function App(){
 const [surah,setSurah]=React.useState('Al-Fatiha'),[mushafStyle,setMushafStyle]=React.useState<MushafStyleId>(defaultMushafStyle),[highlight,setHighlight]=React.useState<keyof typeof highlightColors>('gold'),[speed,setSpeed]=React.useState(50),[translation,setTranslation]=React.useState('English'),[playing,setPlaying]=React.useState(false),[time,setTime]=React.useState(0),[pageUrl,setPageUrl]=React.useState<string|null>(null),[audioUrl,setAudioUrl]=React.useState<string|null>(null),[audioName,setAudioName]=React.useState('Demo timing'),[translationName,setTranslationName]=React.useState('Built-in demo translation')
 const audioRef=React.useRef<HTMLAudioElement>(null),duration=15.7
 const activeIndex=seedQuran.findIndex(a=>time>=a.words[0].start&&time<a.words[a.words.length-1].end),activeAyah=activeIndex>=0?seedQuran[activeIndex]:seedQuran[seedQuran.length-1],activeWordIndex=Math.max(0,activeAyah.words.findIndex(w=>time>=w.start&&time<w.end)),style=mushafStyles.find(s=>s.id===mushafStyle)!
 React.useEffect(()=>{if(!playing)return;const id=window.setInterval(()=>setTime(t=>{const n=t+.03;if(n>=duration){setPlaying(false);return duration}return n}),30);return()=>window.clearInterval(id)},[playing])
 React.useEffect(()=>{if(audioRef.current)audioRef.current.currentTime=time},[time])
 const handleFile=(e:React.ChangeEvent<HTMLInputElement>,cb:(url:string,name?:string)=>void)=>{const f=e.target.files?.[0];if(f)cb(URL.createObjectURL(f),f.name)}
 const togglePlay=()=>{if(audioRef.current){if(playing)audioRef.current.pause();else audioRef.current.play().catch(()=>undefined)}setPlaying(v=>!v)}
 const reset=()=>{setPlaying(false);setTime(0);audioRef.current?.pause()}
 return <div className="app-shell">{audioUrl&&<audio ref={audioRef} src={audioUrl} onPlay={()=>setPlaying(true)} onPause={()=>setPlaying(false)}/>}<header className="topbar"><div><div className="brand">Quran Video Maker</div><div className="subtitle">Quran recitation video studio · synchronization prototype</div></div><div className="top-actions"><button className="ghost"><Settings2 size={16}/> Settings</button><button className="primary" onClick={togglePlay}>{playing?<Pause size={16}/>:<Play size={16}/>} {playing?'Pause':'Preview'}</button></div></header>
 <main className="workspace"><aside className="sidebar left-panel"><section className="panel-section"><div className="section-title"><BookOpen size={16}/> Quran</div><label>Surah</label><select value={surah} onChange={e=>setSurah(e.target.value)}>{surahs.map(s=><option key={s}>{s}</option>)}</select><div className="two-col"><div><label>From ayah</label><input defaultValue="1" type="number" min="1"/></div><div><label>To ayah</label><input defaultValue="7" type="number" min="1"/></div></div><label>Mushaf style</label><select value={mushafStyle} onChange={e=>setMushafStyle(e.target.value as MushafStyleId)}>{mushafStyles.map(s=><option key={s.id} value={s.id}>{s.label}</option>)}</select><small className="hint">{style.description}</small><label>Page source</label><label className="upload-box"><Upload size={18}/><span>{pageUrl?'Quran page loaded':'Upload Quran page'}</span><small>PNG, JPG, WEBP</small><input hidden type="file" accept="image/png,image/jpeg,image/webp" onChange={e=>handleFile(e,u=>setPageUrl(u))}/></label></section>
 <section className="panel-section"><div className="section-title"><Music2 size={16}/> Recitation</div><select><option>Select reciter</option><option>Online source</option><option>Use uploaded audio</option></select><label className="upload-inline"><Upload size={15}/> {audioName}<input hidden type="file" accept="audio/*" onChange={e=>handleFile(e,(u,n)=>{setAudioUrl(u);setAudioName(n||'Uploaded audio');setPlaying(false);setTime(0)})}/></label><small className="hint">Uploaded audio is synchronized to the preview timeline. Verified online audio sources will be added separately.</small></section>
 <section className="panel-section"><div className="section-title"><Languages size={16}/> Translation</div><select value={translation} onChange={e=>setTranslation(e.target.value)}><option>None</option><option>English</option><option>Urdu</option><option>Arabic</option><option>English + Urdu</option><option>English + Urdu + Arabic</option></select><label className="upload-inline"><Upload size={15}/> Upload translation<input hidden type="file" accept=".csv,.json,.txt,.srt,.vtt" onChange={e=>{const f=e.target.files?.[0];if(f)setTranslationName(f.name)}}/></label><small className="hint">{translationName} · CSV, JSON, TXT, SRT, VTT</small></section></aside>
 <section className="preview-area"><div className="preview-toolbar"><span>LIVE PREVIEW</span><span className="status">{playing?'Reciting':'Paused'} · {surah} · {style.label}</span></div><div className="stage-wrap"><div className="stage"><div className="bg-glow"/><div className={`quran-page mushaf-${mushafStyle}`} style={{transform:`translateY(-${time*speed*.16}px)`}}><div className="page-header">سُورَةُ الْفَاتِحَةِ</div>{seedQuran.map((ayah,ai)=><div key={ayah.verseKey} className={`ayah ${ai===activeIndex?'active-line':''}`} style={ai===activeIndex?{'--highlight':highlightColors[highlight]} as React.CSSProperties:undefined}><div className="arabic" dir="rtl">{ayah.words.map((w,wi)=><span key={wi} className={ai===activeIndex&&wi===activeWordIndex?'active-word':''}>{w.text} </span>)}</div>{((translation==='English'||translation.includes('English'))&&ai===activeIndex)&&<div className="translation">{ayah.translations.en}</div>}{((translation==='Urdu'||translation.includes('Urdu'))&&ai===activeIndex)&&<div className="translation urdu" dir="rtl">{ayah.translations.ur}</div>}{((translation==='Arabic'||translation.includes('Arabic'))&&ai===activeIndex)&&<div className="translation arabic-translation" dir="rtl">{ayah.translations.ar}</div>}</div>)}</div>{pageUrl&&<img className="uploaded-page" src={pageUrl} alt="Uploaded Quran page"/>}<div className="finger" style={{left:`${58+activeWordIndex*4}%`,top:`${34+activeIndex*6}%`,transitionDuration:`${Math.max(80,500-speed*3)}ms`}}>☝</div><div className="logo-placeholder">CHANNEL LOGO</div><div className="preview-caption">{activeIndex>=0?`${activeAyah.verseKey} · word ${activeWordIndex+1}`:'Ready'}</div></div></div><div className="transport"><button className="play-circle" onClick={togglePlay}>{playing?<Pause size={18}/>:<Play size={18}/>}</button><span>{formatTime(time)}</span><input className="scrub" type="range" min="0" max={duration} step=".01" value={time} onChange={e=>{setPlaying(false);setTime(Number(e.target.value))}}/><span>{formatTime(duration)}</span><button className="reset" onClick={reset}><RotateCcw size={16}/></button></div><div className="timeline">{['RECITATION','QURAN PAGE','TRANSLATION','HAND + LINE'].map(x=><div key={x}><b>{x}</b><span style={{width:`${time/duration*100}%`}}/></div>)}</div></section>
 <aside className="sidebar right-panel"><section className="panel-section"><div className="section-title"><Hand size={16}/> Focus & Hand</div><label>Scroll speed <strong>{speed}%</strong></label><input type="range" min="0" max="100" value={speed} onChange={e=>setSpeed(Number(e.target.value))}/><div className="switch-row"><span>Moving finger</span><input type="checkbox" defaultChecked/></div><div className="switch-row"><span>Active line highlight</span><input type="checkbox" defaultChecked/></div><label>Highlight</label><div className="color-grid">{Object.keys(highlightColors).map(c=><button key={c} className={`color ${c} ${highlight===c?'selected':''}`} onClick={()=>setHighlight(c as keyof typeof highlightColors)} aria-label={c}/>)}</div><label>Opacity</label><input type="range" defaultValue="25"/></section></aside></main></div>}
function formatTime(s:number){const m=Math.floor(s/60),sec=Math.floor(s%60);return `${String(m).padStart(2,'0')}:${String(sec).padStart(2,'0')}`}
createRoot(document.getElementById('root')!).render(<App/>)
